# .github/workflows/ci.yml
name: Java CI with Allure

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Cache local Maven repository
        uses: actions/cache@v3
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-m2-

      - name: Build & run tests
        run: mvn clean verify -q

      - name: Upload Allure results
        if: always()name: Java CI with Allure

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: '17'

      - name: Build & run tests
        run: mvn clean verify -q

      - name: Upload Allure results
        if: always()
        uses: actions/upload-artifact@v3.1.3    # pin to a concrete patch version
        with:
          name: allure-results
          path: target/allure-results

      - name: Upload Allure HTML report
        if: always()
        uses: actions/upload-artifact@v3.1.3
        with:
          name: allure-report
          path: target/site/allure-maven-plugin

        uses: actions/upload-artifact@v3
        with:
          name: allure-results
          path: target/allure-results

      - name: Generate Allure report
        if: always()
        run: mvn allure:report -q

      - name: Upload Allure HTML report
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: allure-report
          path: target/site/allure-maven-plugin
